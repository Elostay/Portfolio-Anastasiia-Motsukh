import { Cardo, Dancing_Script } from "next/font/google";
import { Space_Grotesk } from "next/font/google";

export const cardoRegular = Cardo({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});

export const dancingRegular = Dancing_Script({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});

export const spaceGroteskRegular = Space_Grotesk({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});
